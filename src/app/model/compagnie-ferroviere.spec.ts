import { CompagnieFerroviere } from './compagnie-ferroviere';

describe('CompagnieFerroviere', () => {
  it('should create an instance', () => {
    expect(new CompagnieFerroviere()).toBeTruthy();
  });
});
