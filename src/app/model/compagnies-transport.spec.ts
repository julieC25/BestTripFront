import { CompagniesTransport } from './compagnies-transport';

describe('CompagniesTransport', () => {
  it('should create an instance', () => {
    expect(new CompagniesTransport()).toBeTruthy();
  });
});
